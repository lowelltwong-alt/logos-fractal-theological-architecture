# Exceptions lake import pack

This directory stores the generated exceptions-lake handoff artifacts for import into the Logos Fractal Theological Architecture repository.

Included here:

- `priority_report.md` — generated sample priority output
- `exceptions-lake-pack-series.patch` — patch series for the standalone pack
- `pack-readme.md` — the pack overview adapted for this repo context

Suggested next move:
- apply the patch contents selectively into the repo's native `docs/`, `schema/`, `examples/`, and `tools/` layout
- then align paths, naming, and vocabulary discipline with the repo's governed architecture

This commit is meant to get the work into the repository immediately without forcing a blind root-level overwrite.
